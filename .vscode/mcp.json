{
  // 💡 Inputs will be prompted on first server start,
  //    then stored securely by VS Code.
  "inputs": [
    {
      "type": "promptString",
      "id": "perplexity-key",
      "description": "Perplexity API Key",
      "password": true
    }
  ],
  "servers": {
    "simpile": {
      "command": "node",
      "args": [
        "../mcp-server-simple/build/index.js" // path to build index.js
      ]
    }
  }
}
